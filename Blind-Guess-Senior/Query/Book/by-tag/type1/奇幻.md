
```dataview
TABLE WITHOUT ID
 file.link AS 书名, country AS 国家, author AS 作者, year AS 年份, score AS 评分, status AS 状态, tags AS 标签
FROM "Blind-Guess-Senior"
WHERE contains(category, "书籍")
WHERE contains(tags, "奇幻")
SORT score DESC, year DESC, file.name ASC
```



